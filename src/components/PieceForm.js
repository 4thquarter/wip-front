{
	/* <form autoComplete='yeeyee'>
					<input
						className='inputBox'
						onChange={props.handleChange}
						type='text'
						id='title'
						name='title'
						autoComplete='yeeyee'
						placeholder='title'
					/>

					<input
						className='inputBox'
						onChange={props.handleChange}
						type='text'
						id='artist'
						name='artist'
						placeholder='artist'
					/>

					<select
						name='colorSelect'
						id='colorSelect'
						onChange={props.handleChange}
						className='select'>
						{colorChoices}
					</select>

					<select
						name='mediumSelect'
						id='mediumSelect'
						onChange={props.handleChange}
						className='select'>
						{mediumChoices}
					</select>

					<button
						className='artSubmit'
						onClick={props.submitArt}
						type='submit'
						name='submit'>
						===>
					</button>
				</form> */
}
